# Digest: 2026-06-04

0 paper(s) found today.

## Search plan

Previous 7 days well-covered: off-policy LLM RL + sample difficulty analysis (June 3 — POPO/T-SAE/PR2), training stability/entropy (June 2 — EP-GRPO/M-GRPO/KL3), multi-turn/long-horizon RL (June 2 — OpenWebRL/BranPO/T²PO/TRM), tool-use RL (June 1 — EchoRL/DARTS/AdaptR1), value/critic LLM RL (June 1 — POISE/DARE). No user annotations found in vault.

Carry-forwards: multi-agent LLM RL (5 consecutive days of rate-limit failures); VLM/GUI computer-use RL (rate-limited May 31, OpenWebRL June 2 for web only). No S2/arxiv hits today.

**Root cause of total failure:** Agent launched 4 parallel background search processes simultaneously, each hammering arxiv API concurrently. arxiv rate-limited our IP immediately and sustained the block for 45+ minutes (still 429 after exhausting the full 65+120+180+300+480+600s = 1745s backoff ladder). S2 also immediately rate-limited (no-API-key public tier).

1. `ti:(multi-agent OR collaborative OR multi-LLM) AND ti:(GRPO OR reinforcement OR reward) AND ti:(language model OR LLM OR reasoning)` via S2 → arxiv — **complete failure**: S2 rate-limited instantly; arxiv 429 through all 6 retry steps totaling ~29 minutes.

2. `ti:(GUI OR computer-use OR desktop OR web agent) AND ti:(reinforcement OR GRPO OR reward) AND ti:(VLM OR vision-language OR multimodal OR agent)` via arxiv — **rate-limited out** (parallel launch with query 1; 65+120+180+240s).

3. `ti:(RLAIF OR AI feedback OR constitutional) AND ti:(reinforcement OR alignment OR training) AND ti:(language model OR LLM OR agent)` via arxiv — **rate-limited out** (parallel launch).

4. `ti:(generalization OR transfer OR out-of-distribution) AND ti:(RLVR OR GRPO OR reward) AND ti:(language model OR LLM OR reasoning)` via arxiv — **rate-limited out** (parallel launch).

**Fix applied:** Extended arxiv retry backoff ladder from [65,120,180,240] to [65,120,180,300,480,600] seconds; added required `User-Agent` header per arxiv API documentation. **Root-cause prevention:** searches must be run strictly sequentially (one at a time) to avoid parallel IP-level blocks. Do NOT launch multiple scanner.search processes simultaneously.

## Papers

No papers met the score threshold today.
