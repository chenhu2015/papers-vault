# Digest: 2026-06-10

5 paper(s) found today.

## Search plan

Recent 7 days well-covered: reward model robustness (HARVE/RefCritic/RM-NLHF/RREDCoT — Jun 6-9), agentic skill internalization (SIRI/ECPO — Jun 8), multi-agent RL (MarsRL/LangMARL — Jun 6), VLM RL (VGS/VURB/SenseNova-MARS — Jun 6/9), GRPO variants (DiScO/ConSteer-RL — Jun 9), OOD generalization (SUPERNOVA/START — Jun 6-7), SAE/mechanistic interpretability meets RL (Tell-Tale Norm — Jun 9), RL timing at pre-training (RL Excursions — Jun 8). No user annotations.

Jun 9 runner-ups explicitly flagged: ASymPO (0.76, async RL without log-probs — 3rd consecutive miss), SCI-PRM (tool-aware PRM), preference data query (failed twice, needs new angle).

**Critical constraint:** all searches run strictly sequentially (one at a time) to avoid IP rate-limit block.

1. `ti:(asynchronous OR off-policy OR importance weight) AND ti:(policy optimization OR reinforcement) AND ti:(language model OR LLM OR reasoning)` via arxiv — ASymPO thread + async/importance-weighted RL for LLMs underexplored since POPO (Jun 3). **Productive**: ALP (0.80, layerwise perturbation for off-policy stability), CTPO (0.79, cumulative token IS ratio), Off-Policy Pessimism (0.78, implicit pessimism framing), OAPL (0.78, off-policy RL without IS). ~30% off-target (stochastic finance, quantization).

2. `ti:(preference dataset OR human feedback OR annotation) AND ti:(RLHF OR alignment OR DPO) AND ti:(scalable OR diverse OR automated)` via arxiv — Jun 9's explicit requery for twice-failed preference data angle. **Off-target again**: attracted human values alignment, AV generation evaluation, advertising image DPO. Best finds: Hölder-DPO (0.72, noisy-label-robust alignment), PRO (0.70, DPO decomposition). Query confirmed as structurally broken — the word "alignment" aggressively matches ethics/values literature. Next attempt needs to drop "alignment" and instead use specific technical terms like `RLHF OR DPO OR preference optimization`.

3. `ti:(long-context OR retrieval OR memory) AND ti:(reinforcement OR reward OR training) AND ti:(language model OR LLM OR agent)` via arxiv — Retrieval/long-context RL for agents; not covered in recent weeks. **Very productive**: MemoPilot (0.80, RL over memory updates with multi-turn GRPO), InfoMem (0.80, answer-conditioned memory rewards), CERL (0.78, counterfactual erasure RL for persistent-state distinction), Search Agent Training Study (0.78, controlled study of search agent RL). ~35% off-target (KV cache compression, quantization, satellite retrieval).

4. `ti:(chain-of-thought OR reasoning trace OR thinking tokens) AND ti:(training OR reinforcement OR distillation OR reward) AND ti:(language model OR LLM)` via arxiv — CoT/trace quality as RL training signal. **Productive**: CPPO (0.82, cumulative prefix trust region for RLVR), DiRL (0.80, direction-aware exploration reasoning vs memorization), CAPR (0.75, RL for diffusion LLMs with trace-level block rewards). ~40% off-target (TTS, clinical NLP, robotics VLA).

**All 5 digest papers from queries 1, 3, and 4.** CPPO (0.82) is the standout: the cleanest theoretical advance on PPO trust-region design for autoregressive LLMs — the cumulative prefix budget insight directly addresses why uniform clipping is structurally wrong for sequence generation. DiRL (0.80) is the most conceptually novel: distinguishing reasoning vs memorization as a gradient-direction signal and using it to shape GRPO rewards. MemoPilot and InfoMem together open a new memory-augmented agentic RL cluster — MemoPilot handles the policy-training side (what to memorize), InfoMem handles the reward-design side (whether the memory helped). ALP (0.80) is an important practical result for off-policy stability: representation-level perturbation as an alternative to IS correction unifies the instability problem across training architectures.

Tomorrow: CTPO (0.79, cumulative IS ratio with position-adaptive clipping — exact theoretical complement to CPPO), Off-Policy Pessimism (0.78, implicit pessimism framing for removing IS weights), CERL (0.78, counterfactual erasure RL — counterfactual persistent-state test as RL reward), Search Agent Training Study (0.78, controlled empirical study of search agent RL including data coverage, outcome vs process reward, off-policy data). Preference data query needs fundamental restructuring: drop "alignment", try `ti:(preference data OR preference pairs OR pairwise comparison) AND ti:(RLHF OR DPO OR reward model) AND ti:(construction OR generation OR quality OR noise)`.

## Papers

- [[2026-06-10-beyond-uniform-token-level-trust-region-in-llm-reinforcement]] — score: 0.82 — CPPO introduces position-weighted clipping and cumulative prefix budget to fix PPO's structurally position-agnostic trust region for autoregressive LLM RL
- [[2026-06-10-reasoning-or-memorization-direction-aware-diversity-in-llm-rl]] — score: 0.80 — DiRL shapes GRPO exploration rewards using an internal reasoning-vs-memorization gradient direction to amplify genuine reasoning diversity and suppress memorization shortcuts
- [[2026-06-10-memopilot-test-time-memory-learning-for-frozen-llm-agents]] — score: 0.80 — MemoPilot trains a memory-update copilot via multi-turn GRPO alongside a frozen LLM agent, enabling progressive test-time learning without modifying the player
- [[2026-06-10-infomem-answer-conditioned-rewards-for-long-context-memory-rl]] — score: 0.80 — InfoMem rewards chunk-wise memory agents by measuring how much accumulated memory increases answer log-likelihood, directly supervising memory utility under GRPO
- [[2026-06-10-adaptive-layerwise-perturbation-off-policy-llm-rl-stability]] — score: 0.80 — ALP injects learnable hidden-state perturbations per layer to flatten the importance-ratio distribution, reducing KL spikes and heavy tails in off-policy LLM RL training
