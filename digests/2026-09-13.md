# Digest: 2026-09-13

0 paper(s) found today.

## Search plan

Based on Sep 12 carry-forward queries and open gaps from the hindsight-credit thread.

1. **Harness-RL / CAPO gradient routing** (Sep 12 carry-forward): `all:(conflict aware gradient routing action args policy optimization multi-agent harness language model reinforcement)` via arxiv abstract search — targeting papers adjacent to CAPO (Conflict-Aware Policy Optimization) and action/args gradient routing in multi-agent harnesses.

2. **SFT-free VLM RL cold-start** (Sep 12 carry-forward): `ti:(cold start OR SFT-free OR warm-up free) AND ti:(VLM OR vision language OR multimodal OR visual) AND ti:(GRPO OR reinforcement OR RL) AND ti:(agent OR agentic OR reasoning OR tool)` via arxiv — fresh angle targeting pure online GRPO for VLMs without SFT warm-up.

3. **CANOPY replication / coverage-anchored RL** (Sep 12 carry-forward): `ti:(exploration OR coverage) AND ti:(outcome OR sparse reward) AND ti:(long-horizon OR multi-turn OR agentic) AND ti:(reinforcement OR RL OR GRPO) AND ti:(language model OR LLM OR agent)` via arxiv — testing the test-time budget vs. training signal tradeoff empirically.

4. **Latent credit / LTC thread** (new): `ti:(latent OR continuous OR implicit) AND ti:(credit OR advantage OR reward) AND ti:(reasoning OR thought OR chain) AND ti:(reinforcement OR GRPO OR RL) AND ti:(language model OR LLM)` via arxiv — targeting LTC-adjacent work extending GRPO credit to latent/continuous reasoning representations.

**All 4 searches failed: arxiv IP rate-limited throughout the entire run.**

Root cause: the 4 searches were launched in parallel at ~08:00–08:06 UTC, collectively saturating the arxiv IP rate limit immediately. Every initial request (attempt 0) received a 429, as did all 6 subsequent backoff retries per search (backoff ladder: 65s, 120s, 180s, 300s, 480s, 600s — total ~29 minutes per search). The 7th and final attempt also returned 429 for Search 1 (confirmed 08:29 UTC); Searches 2–4 expected to fail identically in the next 8 minutes.

Semantic Scholar also rate-limited when queried independently (429 returned, no retry per client design).

**Action for next run**: launch searches sequentially with at least a 3-second gap between requests to avoid triggering arxiv's per-IP rate limit. Do not fire multiple scanner.search processes in parallel.

**No papers found. No vault notes written.**

## Open gaps (carried forward from 2026-09-12)

**SAPO+SAO+SRPO combination**: definitively closed — the three-way GRPO extension composition remains unstudied.

**SFT-free VLM RL core gap** (VLM + offline preference + no-SFT cold-start for agentic tool use): still open after Sep 12 search attempts.

**Hindsight-informed credit redistribution unified framework** (TRIAL+T-STAR+TASPO+CRISP convergence): no paper providing unified treatment or empirical comparison found.

**Model-harness joint optimization**: Harness-RL (2608.29641v1, 0.82, excluded by cap Sep 12) — Conflict-Aware Policy Optimization + action/args gradient routing in multi-agent harnesses. Gap partially narrowing.

**CANOPY vs. dense credit empirical question**: is the test-time interaction budget expansion in CANOPY load-bearing, or does coverage-anchoring alone explain the gain?

**LTC** (2608.01593v1, 0.80, excluded by cap Sep 12): Latent Thought Credit — extends GRPO credit to continuous latent reasoning via multi-answer thought-level expected reward; analogous to CRISP's backward evidence induction in latent space; worth reading in context of CompPO.

## Tomorrow

Retry all 4 searches from today sequentially (not in parallel):
1. Harness-RL CAPO gradient routing abstract sweep
2. SFT-free VLM RL cold-start (GRPO without SFT warm-up)
3. CANOPY replication / coverage-anchored RL
4. Latent credit / LTC thread

Use `--source arxiv` only and fire with ≥3s between each call, or use `--source s2` as fallback if arxiv is still rate-limited.
