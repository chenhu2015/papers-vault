# Digest: 2026-09-14

0 paper(s) found today.

## Search plan

Carried forward all 4 searches from the Sep 13 failure (which was itself a carry-forward from Sep 12). All 4 searches blocked by persistent arxiv IP ban.

1. **Harness-RL / CAPO gradient routing**: `all:(conflict aware gradient routing action args policy optimization multi-agent harness language model reinforcement)` via arxiv abstract search.

2. **SFT-free VLM RL cold-start**: `ti:(cold start OR SFT-free OR warm-up free) AND ti:(VLM OR vision language OR multimodal OR visual) AND ti:(GRPO OR reinforcement OR RL) AND ti:(agent OR agentic OR reasoning OR tool)` via arxiv.

3. **CANOPY replication / coverage-anchored RL**: `ti:(exploration OR coverage) AND ti:(outcome OR sparse reward) AND ti:(long-horizon OR multi-turn OR agentic) AND ti:(reinforcement OR RL OR GRPO) AND ti:(language model OR LLM OR agent)` via arxiv.

4. **Latent credit / LTC thread**: `ti:(latent OR continuous OR implicit) AND ti:(credit OR advantage OR reward) AND ti:(reasoning OR thought OR chain) AND ti:(reinforcement OR GRPO OR RL) AND ti:(language model OR LLM)` via arxiv.

**All searches blocked: arxiv IP rate-limited for a second consecutive day.**

Root cause: the Sep 13 parallel-search incident caused a multi-day arxiv IP ban. On Sep 14, Search 1's very first HTTP request (attempt 0) returned 429. The backoff ladder (65s, 120s, 180s, 300s, 480s, 600s) was exhausted; the 7th and final attempt also returned 429, confirming the ban was still fully active after 24+ hours. Searches 2–4 were not launched (no point exhausting another ~29 minutes per search against an active multi-day ban). S2 was also rate-limited on Sep 13 when tested as fallback.

**Revised understanding**: the arxiv parallel-search IP ban is not a 30-minute cooldown but a multi-day ban. The IP may need 48–72 hours to clear, or the ban is tied to a rolling request-rate window that resets more slowly than anticipated.

**No papers found. No vault notes written.**

## Open gaps (carried forward from 2026-09-13)

**SAPO+SAO+SRPO combination**: definitively closed — the three-way GRPO extension composition remains unstudied.

**SFT-free VLM RL core gap** (VLM + offline preference + no-SFT cold-start for agentic tool use): still open after Sep 12–14 search attempts.

**Hindsight-informed credit redistribution unified framework** (TRIAL+T-STAR+TASPO+CRISP convergence): no paper providing unified treatment or empirical comparison found.

**Model-harness joint optimization**: Harness-RL (2608.29641v1, 0.82, excluded by cap Sep 12) — Conflict-Aware Policy Optimization + action/args gradient routing in multi-agent harnesses. Gap partially narrowing.

**CANOPY vs. dense credit empirical question**: is the test-time interaction budget expansion in CANOPY load-bearing, or does coverage-anchoring alone explain the gain?

**LTC** (2608.01593v1, 0.80, excluded by cap Sep 12): Latent Thought Credit — extends GRPO credit to continuous latent reasoning via multi-answer thought-level expected reward; analogous to CRISP's backward evidence induction in latent space; worth reading in context of CompPO.

## Tomorrow

Retry all 4 searches from today sequentially (not in parallel). arxiv ban may persist into Sep 15 — if Search 1 attempt 0 returns 429 again, skip immediately to S2 fallback rather than waiting through the full backoff ladder:

1. Harness-RL CAPO gradient routing abstract sweep
2. SFT-free VLM RL cold-start (GRPO without SFT warm-up)
3. CANOPY replication / coverage-anchored RL
4. Latent credit / LTC thread

If arxiv ban persists, use `--source s2` for all 4 searches.
