# Digest: 2026-09-15

0 paper(s) found today.

## Search plan

Carried forward all 4 searches from Sep 12–14 (three consecutive failed days). arxiv ban is confirmed still active on Sep 15 (day 3+). S2 also persistently rate-limited.

1. **Harness-RL / CAPO gradient routing**: `all:(conflict aware gradient routing action args policy optimization multi-agent harness language model reinforcement)` — attempted arxiv (background task, 5/7 retries all 429) and S2 (rate limited). S2 briefly opened and returned 1 off-target result (FlowBender, diffusion flows).

2. **SFT-free VLM RL cold-start**: `ti:(cold start OR SFT-free OR warm-up free) AND ti:(VLM OR vision language OR multimodal OR visual) AND ti:(GRPO OR reinforcement OR RL) AND ti:(agent OR agentic OR reasoning OR tool)` — S2 rate limited for all attempts.

3. **CANOPY replication / coverage-anchored RL**: `ti:(exploration OR coverage) AND ti:(outcome OR sparse reward) AND ti:(long-horizon OR multi-turn OR agentic) AND ti:(reinforcement OR RL OR GRPO) AND ti:(language model OR LLM OR agent)` — S2 rate limited; not reached.

4. **Latent credit / LTC thread**: `ti:(latent OR continuous OR implicit) AND ti:(credit OR advantage OR reward) AND ti:(reasoning OR thought OR chain) AND ti:(reinforcement OR GRPO OR RL) AND ti:(language model OR LLM)` — S2 rate limited; not reached.

**arxiv ban status**: Confirmed still active on Sep 15. Background task launched on Search 1 arxiv query at ~08:00 UTC; 5 consecutive 429s (attempts 0–4: waited 65s, 120s, 180s, 300s, 480s). The ban is now confirmed at ≥72 hours since the Sep 13 parallel-search incident.

**S2 status**: Also rate-limited today. Briefly opened for ~1 request window (returned off-target result for Search 1), then locked again for extended periods (>5 minutes of polling). S2 rate-limiting pattern is consistent with an active block rather than normal per-request throttle.

**Both primary and fallback sources blocked for the 3rd consecutive day.**

**No papers found. No vault notes written.**

## Open gaps (carried forward from 2026-09-12)

**SAPO+SAO+SRPO combination**: definitively closed — the three-way GRPO extension composition remains unstudied.

**SFT-free VLM RL core gap** (VLM + offline preference + no-SFT cold-start for agentic tool use): still open after Sep 12–15 search attempts.

**Hindsight-informed credit redistribution unified framework** (TRIAL+T-STAR+TASPO+CRISP convergence): no paper providing unified treatment or empirical comparison found.

**Model-harness joint optimization**: Harness-RL (2608.29641v1, 0.82, excluded by cap Sep 12) — Conflict-Aware Policy Optimization + action/args gradient routing in multi-agent harnesses. Gap partially narrowing.

**CANOPY vs. dense credit empirical question**: is the test-time interaction budget expansion in CANOPY load-bearing, or does coverage-anchoring alone explain the gain?

**LTC** (2608.01593v1, 0.80, excluded by cap Sep 12): Latent Thought Credit — extends GRPO credit to continuous latent reasoning via multi-answer thought-level expected reward; analogous to CRISP's backward evidence induction in latent space; worth reading in context of CompPO.

## Updated understanding of the rate-limit situation

The Sep 13 parallel-search incident resulted in a multi-day arxiv IP ban now confirmed to extend beyond 72 hours (still active Sep 15). S2 also became rate-limited on Sep 13 and remains so.

**Working hypothesis**: The arxiv ban may be tied to a rolling request-rate window or a longer per-IP blacklist period than originally assumed. The S2 rate limit may be similarly tied to high-frequency query bursts on Sep 13.

**Action required**: Allow both APIs additional recovery time. If the ban persists into Sep 16, consider:
1. Testing arxiv with a minimal probe query (1 request, no retries) at the start of each run before committing to the full backoff ladder.
2. Testing S2 with a minimal probe before each search attempt.
3. Identifying whether the IP address itself needs to change (contact OCI support or use a NAT gateway to rotate the IP).

## Tomorrow

Retry all 4 searches from today. Probe arxiv with a single minimal query first (1 request, no retries, no backoff ladder) — if 200, proceed; if 429, skip to S2. Probe S2 similarly before each query attempt.

1. Harness-RL CAPO gradient routing
2. SFT-free VLM RL cold-start (GRPO without SFT warm-up)
3. CANOPY replication / coverage-anchored RL
4. Latent credit / LTC thread

If both remain blocked, investigate IP rotation options via OCI VM settings.
