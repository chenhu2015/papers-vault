# Digest: 2026-09-23

2 paper(s) found today.

## Search plan

**Orientation**: No user annotations found in vault (all `## My Thoughts` sections remain empty). Following all 4 "Tomorrow" queries from the 2026-09-22 digest exactly. Open threads carried: (1) open-ended agent RL reward design via intrinsic motivation angle, (2) CoSkill×ArenaFlow tournament+skill comparison, (3) ECHO credit routing follow-up (intra-trajectory credit attribution), (4) two-level curriculum joint treatment (within-task + across-task).

Searches executed (all via arxiv, sequential; S2 rate-limited throughout):

1. **Open-ended reward via intrinsic motivation**: `ti:(exploration bonus OR intrinsic reward OR self-play) AND ti:(language model OR LLM) AND ti:(open-ended OR diverse OR multi-task) AND ti:(reinforcement OR GRPO OR RL)` — 2 results; **Omni-Thinker** (2507.14783v3, 0.80) surfaced — uses LLM-as-Judge hybrid reward for open-ended domains rather than intrinsic bonuses; fills gap from a practical angle; old multi-task RL paper from 2016 filtered out
2. **CoSkill×ArenaFlow tournament+skill comparison**: `ti:(tournament OR ranking OR ELO OR rating) AND ti:(skill OR memory OR library) AND ti:(agent OR agentic) AND ti:(reinforcement OR GRPO OR policy)` — 0 results; gap remains open; no paper combines tournament reward with explicit skill stores
3. **ECHO credit routing follow-up**: `ti:(credit assignment OR credit propagation OR provenance) AND ti:(multi-turn OR long-horizon OR context) AND ti:(agent OR agentic) AND ti:(reinforcement OR GRPO OR RL)` — 0 results (arxiv backoff triggered, all results previously seen or empty); gap persists
4. **Two-level curriculum joint treatment**: `ti:(curriculum OR scheduling) AND ti:(multi-task OR multi-domain OR multi-capability) AND ti:(language model OR LLM OR agent) AND ti:(reinforcement OR GRPO OR training)` — 2 results; **PAC** (2608.30528v1, 0.87) surfaced — directly fills the MATCH+HarnessBandit gap by jointly tracking advantage-derived learnability AND reward gains via Thompson Sampling; Spindle (distributed training system) off-topic

## Papers

- [[2026-08-31-pac-progress-augmented-advantage-curriculum-for-multi-task-r]] — score: 0.87 — PAC: dual-signal curriculum (advantage magnitude + reward delta) allocated via Bayesian Thompson Sampling during GRPO; jointly addresses within-task learnability and across-task performance tracking — fills the MATCH+HarnessBandit gap
- [[2025-07-20-omni-thinker-scaling-multi-task-rl-in-llms-with-hybrid-rewar]] — score: 0.80 — Omni-Thinker: hybrid reward (rule-based + LLM-as-Judge) + BWT-guided scheduling for simultaneous structured-reasoning and open-ended-generation training across 4 domains; 6.2% over joint training

## Open gaps (updated)

**CoSkill × ArenaFlow tournament+skill comparison**: Query 2 returned 0 results for the second time with different formulation. The open empirical comparison between tournament-based reward (ArenaFlow) and jointly-learned skill manager (CoSkill) for open-ended agent RL appears genuinely absent. De-prioritize; revisit if a survey paper on skill memory for agents surfaces.

**ECHO credit routing / intra-trajectory credit attribution**: Query 3 triggered arxiv backoff and returned 0 results from seen papers. Gap persists but may be saturated in the seen-papers DB — consider a reformulation next time targeting specific mechanisms: `ti:(source attribution OR turn-level reward OR step reward OR hindsight) AND ti:(multi-turn OR long-horizon) AND ti:(language model OR LLM OR agent)`.

**Two-level curriculum resolved by PAC**: PAC fills the within-task (advantage) + across-task (reward delta) gap jointly. The remaining question is whether PAC + MATCH (within-task difficulty gating) + HarnessBandit (which harness) can be unified into a three-level curriculum hierarchy. No paper addresses this.

**Open-ended reward intrinsic motivation angle**: Gap partially addressed from the practical angle (Omni-Thinker's LLM-as-Judge hybrid reward). The pure intrinsic bonus angle (exploration bonus, self-play, curiosity) for LLM open-ended tasks still has no strong hits. De-prioritize in favor of more productive threads.

## Tomorrow

1. **ECHO credit reformulation**: `ti:(source attribution OR turn-level reward OR step reward OR hindsight) AND ti:(multi-turn OR long-horizon) AND ti:(language model OR LLM OR agent) AND ti:(reinforcement OR GRPO OR RL)` — reframed from "credit propagation" to specific mechanism terms
2. **PAC × MATCH × HarnessBandit three-level unification**: `ti:(task selection OR task sampling OR rollout allocation) AND ti:(curriculum OR learnability OR difficulty) AND ti:(multi-task OR multi-domain) AND ti:(language model OR LLM OR agent)` — look for papers covering both which-task and how-hard simultaneously
3. **Omni-Thinker BWT scheduling follow-up**: `ti:(backward transfer OR forward transfer OR catastrophic forgetting) AND ti:(reinforcement OR GRPO OR RL) AND ti:(language model OR LLM OR agent) AND ti:(multi-task OR continual OR sequential)` — BWT-guided scheduling as a forgetting-prevention mechanism for LLM RL post-training
4. **Reward model efficiency / scalability for multi-task RL**: `ti:(reward model OR reward function) AND ti:(efficient OR scalable OR lightweight) AND ti:(multi-task OR heterogeneous OR diverse) AND ti:(language model OR LLM OR agent)` — as Omni-Thinker uses LLM-as-Judge which is expensive, look for cheaper reward proxies for open-ended tasks
